%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%Apêndice B
\hypertarget{estilo:apendice}{} %% uso para este Guia
%Este apêndice foi criado apenas para indicar como construir um apêndice no estilo, não existia no original da tese.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\renewcommand{\thechapter}{}%
\chapter{APPENDIX B - TRACKING ALGORITHM}	% trocar A por B na próxima apêndice e etc
\label{apendiceB}	% trocar A por B na próxima apêndice e etc
\renewcommand{\thechapter}{B}%		% trocar A por B na próxima apêndice e etc

In this appendix, we will discuss how the algorithm for tracking  was conceived. All codes concerning this dissertation are available on the author's Github: \url{https://github.com/biancafusi/Dissertation}. A brief explanation of the steps of how this code works will be provided below.


Inicialmente partimos dos dados do IBTrACS. A Figura B.1, mostra os pontos de minimo Mean Sea Level Pressure (MSLP) plotados durante todo o período do Beryl.

Initially, we gathered the data from IBTrACS. Figure \ref{fig:appendix3} shows the points ploted during the whole period that Hurricane Beryl was active, where the MLSP is minimal.

\begin{figure}[!ht]
	\centering
	\caption{Tracking of Hurricane Beryl. 
	} % Título acima da figura
	\includegraphics[scale=0.4]{docs/figuras/appendix/B_1_Beryl_tracking.png} 
	\vspace{2mm}
	
	Source:  Made by the author (2025).  % Fonte abaixo da imagem
	\label{fig:appendix3} % Label para referenciar no texto
\end{figure}

As for the code, the initial step is to store the coordinates (latitude and longitude). For that purpose, the variables \texttt{lat\_points\_NOAA} and \texttt{lon\_points\_NOAA} were created, studying the period from July 3rd to July 9th. Following, there will be slices of the original code, concering each step explained.

\lstinputlisting{docs/figuras/appendix/B_fist_code.py}

Having these values, we can draw an initial box around these points. Figure \ref{fig:appendix4} shows how these boxes appear on the tracking.

\begin{figure}[!ht]
	\centering
	\caption{Tracking of Hurricane Beryl, first step of the code. 
	} % Título acima da figura
	\includegraphics[scale=0.4]{docs/figuras/appendix/B_2_Checking_MSLP.png} 
	\vspace{2mm}
	
	Source:  Made by the author (2025).  % Fonte abaixo da imagem
	\label{fig:appendix4} % Label para referenciar no texto
\end{figure}

The following slice of code describes the process to draw these boxes:

\lstinputlisting{docs/figuras/appendix/B_second_code.py}

One should notice the \texttt{upper\_lat}; \texttt{lower\_lat} and \texttt{left\_lon}; \texttt{right\_lon}, which are the limits of these box, are made using the information from \texttt{dlat} and \texttt{dlon}.

The next slice of code searches inside these boxes, looking for the minimal value of MSLP from the referred dataset, for instance, ERA5 or MONAN outputs:

\lstinputlisting{docs/figuras/appendix/B_third_code.py}

Once this minimal value is found, we create a second box around this value in order to find the maximum wind speed. Figure \ref{fig:appendix5} shows how these process appears in the tracking.

\begin{figure}[!ht]
	\centering
	\caption{Tracking of Hurricane Beryl, second step of the code. 
	} % Título acima da figura
	\includegraphics[scale=0.4]{docs/figuras/appendix/B_3_Finding_wind.png} 
	\vspace{2mm}
	
	Source:  Made by the author (2025).  % Fonte abaixo da imagem
	\label{fig:appendix5} % Label para referenciar no texto
\end{figure}

For the second boxes, the following slice of code demonstrates the process.

\lstinputlisting{docs/figuras/appendix/B_fourth_code.py}

The first steps are to save the values from lat and longitude tied to the minimum MSLP, and then, the second box is limited by the lines that specificates the WIND parameters, \texttt{upper\_lat\_WIND}; \texttt{lower\_lat\_WIND}; \texttt{left\_lon\_WIND}; \texttt{right\_lon\_WIND}, defined yet again in terms of \texttt{dlat} and \texttt{dlon}.

Figure \ref{fig:appendix6} shows the next step, which is searching inside the box for the maximum wind speed.

\newpage 

\begin{figure}[!ht]
	\centering
	\caption{Tracking of Hurricane Beryl, third step of the code. 
	} % Título acima da figura
	\includegraphics[scale=0.4]{docs/figuras/appendix/B_4_Finding_Max_wind.png} 
	\vspace{2mm}
	
	Source:  Made by the author (2025).  % Fonte abaixo da imagem
	\label{fig:appendix6} % Label para referenciar no texto
\end{figure}

The code for this part is the following:

\lstinputlisting{docs/figuras/appendix/B_fifth_code.py}

One could ask oneself, what value for \texttt{dlat} and \texttt{dlon} must be imposed. Some tests were done, and we noticed that we cannot restrict a single value for all types of dataset. For instance, one could look at the trajectory on Figure \ref{fig:appendix7}.

\begin{figure}[!ht]
	\centering
	\caption{Placeholder. 
	} % Título acima da figura
	\includegraphics[scale=0.4]{docs/figuras/appendix/failed_track.png} 
	\vspace{2mm}
	
	Source:  Placeholder.  % Fonte abaixo da imagem
	\label{fig:appendix7} % Label para referenciar no texto
\end{figure}

From what we gather on Figure \ref{fig:appendix7}, the algorithm is able to capture the event, but fails after a certain point.

In order to solve this issue, we plotted the curves generated by the tracking above the rainfall path accumulated on the whole period. Figure \ref{fig:appendix8} shows this solution:

\newpage

\begin{figure}[!ht]
	\centering
	\caption{Placeholder. 
	} % Título acima da figura
	\includegraphics[scale=0.5]{docs/figuras/appendix/corrected_track.png} 
	\vspace{2mm}
	
	Source:  Placeholder.  % Fonte abaixo da imagem
	\label{fig:appendix8} % Label para referenciar no texto
\end{figure}

Based on this correction, we can define the size for \texttt{dlat} and \texttt{dlon}. We recommend that this process is done, in order to use this tracking.

Furthermore, we can verify, for example, if the behavior of the intensity registered by ERA5 was not related with a possible tracking error. We can confirm this by confirming the trajectory, shown on Figure \ref{fig:appendix9}:

\newpage

\begin{figure}[!ht]
	\centering
	\caption{Placeholder. 
	} % Título acima da figura
	\includegraphics[scale=0.5]{docs/figuras/appendix/ERA5_track_NOAA.png} 
	\vspace{2mm}
	
	Source:  Placeholder.  % Fonte abaixo da imagem
	\label{fig:appendix9} % Label para referenciar no texto
\end{figure}

One could register the associated values to the minimum MSLP and maximum wind speed in a table, and compare it directly with the data from the best track. Figure \ref{fig:appendix10} shows this table, where the best track data is labeled ``NOAA''.

\newpage

\begin{figure}[!ht]
	\centering
	\caption{Placeholder. 
	} % Título acima da figura
	\includegraphics[scale=0.4]{docs/figuras/appendix/era5_verification_table.png} 
	\vspace{2mm}
	
	Source:  Placeholder.  % Fonte abaixo da imagem
	\label{fig:appendix10} % Label para referenciar no texto
\end{figure}

Thus, one can realize that the tracking is in line with the rainfall accumulated trajectory, and the values associated with it are indeed low. Since the tracking searches in a wide box, we believe that the intensity problem from ERA5 is indeed an issue in the reanalysis.




















